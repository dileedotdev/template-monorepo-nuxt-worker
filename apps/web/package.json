{
  "name": "web",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "type:check": "nuxt typecheck",
    "deploy:development": "NITRO_PRESET=cloudflare nuxt build && wrangler deploy --env development",
    "deploy:preview": "NITRO_PRESET=cloudflare nuxt build && wrangler deploy --env preview",
    "deploy:production": "NITRO_PRESET=cloudflare nuxt build && wrangler deploy --env production"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18.17.5",
    "nuxt": "^3.6.5",
    "wrangler": "^3.5.0"
  }
}
