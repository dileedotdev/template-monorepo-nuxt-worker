{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env local --port 3001",
    "preview": "wrangler dev --env local --port 3001",
    "deploy:development": "wrangler deploy --env development",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@trpc/server": "^10.37.1",
    "@types/lodash-es": "^4.17.8",
    "lodash-es": "^4.17.21",
    "nanoid": "^4.0.2",
    "valibot": "^0.12.0"
  },
  "devDependencies": {
    "wrangler": "^3.5.1"
  }
}
