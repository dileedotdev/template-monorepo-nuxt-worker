{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 3001",
    "preview": "wrangler dev --port 3001",
    "deploy:development": "wrangler deploy --env development",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@trpc/server": "^10.37.1",
    "valibot": "^0.12.0"
  },
  "devDependencies": {
    "wrangler": "^3.5.0"
  }
}
