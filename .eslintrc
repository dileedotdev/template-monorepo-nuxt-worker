{
  "extends": "@antfu",
  "rules": {
    "max-len": ["warn", 120],
    "n/prefer-global/process": "off"
  },
  "ignorePatterns": [
    ".*"
  ],
  "overrides": [
    {
      "files": ["*.json"],
      "rules": {
        "max-len": "off"
      }
    }
  ]
}